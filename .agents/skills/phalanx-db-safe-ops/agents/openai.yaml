interface:
  display_name: 'Phalanx DB Safe Ops'
  short_description: 'Protect dev/test database boundaries'
  default_prompt: 'Use $phalanx-db-safe-ops before running any Phalanx Duel database, migration, seed, psql, or DB-backed test command.'
policy:
  allow_implicit_invocation: true
