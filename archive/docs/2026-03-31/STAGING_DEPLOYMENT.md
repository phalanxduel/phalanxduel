# Staging Deployment Guide

This file is now a secondary pointer. Use these canonical docs instead:

- `docs/deployment/DEPLOYMENT_CHECKLIST.md` for the current staging verification
  and promotion checklist
- `docs/operations/CI_CD_PIPELINE.md` for the active GitHub Actions staging
  workflow
- `fly.staging.toml` for the live staging configuration

This older staging guide remains only so existing links do not break during the
documentation cleanup tranche.
