# Fly.io Staging Setup - Setup & Deploy Instructions

This file is now a secondary pointer. Use these canonical docs instead:

- `docs/deployment/DEPLOYMENT_CHECKLIST.md` for the current staging and
  production deployment checklist
- `docs/operations/CI_CD_PIPELINE.md` for the automated staging deploy path
- `fly.staging.toml` and `scripts/setup-staging.sh` for the live config and
  helper script

This older setup note remains only so existing links do not break during the
documentation cleanup tranche.
