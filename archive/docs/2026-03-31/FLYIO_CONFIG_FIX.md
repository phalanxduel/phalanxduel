# Fly.io Configuration Fix

This file is now a historical note pointer. The live deployment truth is in:

- `fly.staging.toml`
- `fly.production.toml`
- `docs/deployment/DEPLOYMENT_CHECKLIST.md`
- `docs/operations/CI_CD_PIPELINE.md`

Retain this file only as context for older hardening references until the
archival pass decides whether to archive or remove it.
