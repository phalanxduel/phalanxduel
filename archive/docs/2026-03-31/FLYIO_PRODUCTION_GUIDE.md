# Fly.io Production Deployment Guide

This file is now a secondary pointer. Use these canonical docs instead:

- `docs/deployment/DEPLOYMENT_CHECKLIST.md` for the current production deploy
  checklist
- `docs/operations/CI_CD_PIPELINE.md` for the actual GitHub Actions promotion
  flow
- `docs/system/OPERATIONS_RUNBOOK.md` for rollback, migration triage, and
  secrets response
- `fly.production.toml` for the live production configuration

This older Fly-specific guide remains only so existing links do not break
during the documentation cleanup tranche.
