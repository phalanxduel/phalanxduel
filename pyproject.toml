[project]
name = "phalanxduel-python-tools"
version = "0.1.0"
description = "Python tooling helpers for repo-local AI agent workflows."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
  "PyYAML>=6.0.2,<7",
]

[tool.uv]
package = false
